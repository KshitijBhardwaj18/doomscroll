# ===================================
# DOOMSCROLL SERVER CONFIGURATION
# ===================================

# Server Port
PORT=3000

# Node Environment (development, production, or test)
NODE_ENV=development

# ===================================
# DATABASE CONFIGURATION
# ===================================

# PostgreSQL Connection String
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Option 1: Local PostgreSQL (Recommended for development)
DATABASE_URL="postgresql://postgres@localhost:5432/doomscroll"

# Option 2: Docker PostgreSQL
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/doomscroll"

# Option 3: Remote/Cloud PostgreSQL
# DATABASE_URL="postgresql://username:password@your-db-host.com:5432/doomscroll"

# ===================================
# SOLANA BLOCKCHAIN CONFIGURATION
# ===================================

# Solana RPC URL
# Devnet (for development)
SOLANA_RPC_URL="https://api.devnet.solana.com"

# OR use a custom RPC for better performance:
# SOLANA_RPC_URL="https://rpc.helius.xyz/?api-key=YOUR_API_KEY"

# Solana Network (devnet, testnet, or mainnet-beta)
SOLANA_NETWORK="devnet"

# Your deployed program ID (from anchor build)
# Replace with your actual program ID after deployment
PROGRAM_ID="Ae8MNwoRtR9R816pBVg5cWC5nbjTdu7pY394thVZMzzR"

# ===================================
# WALLET & KEYPAIR CONFIGURATION
# ===================================

# Path to verifier keypair JSON file
# This wallet signs transactions and verifies challenges
VERIFIER_KEYPAIR_PATH="./keys/verifier-keypair.json"

# To create a new keypair:
# 1. mkdir -p server/keys
# 2. solana-keygen new --outfile server/keys/verifier-keypair.json
# 3. Get devnet SOL: solana airdrop 2 <YOUR_PUBLIC_KEY> --url devnet

# ===================================
# ADMIN CONFIGURATION
# ===================================

# Admin Secret Key (for challenge creation)
# Generate a secure random string
ADMIN_SECRET_KEY="your-super-secret-admin-key-change-this-in-production"

# To generate a secure key, run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"